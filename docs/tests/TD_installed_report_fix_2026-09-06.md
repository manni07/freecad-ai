# Test dossier: installed report fix

## Current verified outcome

**PASS_DISK_ONLY_RUNTIME_NOT_RELOADED.** The corrected combined suite passed1992 tests plus3 subtests in182.39 s (JUnit1995, zero failures/errors/skips). Selected native preflight checks passed10 in24.91 s. Exact combined native GUI passed with13 imported-module hashes and normal exit0. Root integrated ten scoped files: seven expected baseline changes plus three new files;337 other baseline files are unchanged. Actual installed-target tests passed63 in9.77 s, zero skips. Authenticated MCP check and read-only document inventory still pass. Live activation remains HOLD: no reload, stop or restart was performed. Eight genuine recovery-GUI cases passed with normal exit0 and a confirmed ReportOutput widget: legacy defect reproduced, candidate valid/status/negative/accept controls passed. Four additional path variants and exhaustive geometry remain NOT RUN. The native candidate is not installed.

Defined before implementation,2026-09-06. [Predecessor TD](TD_report_errors_2026-09-05.md) provides historical1642-test/native evidence only. Final corrected combined run:1992 passed plus3 subtests in182.39 s, JUnit1995, zero failures/errors/skips. The first-run failure is preserved below as diagnostic history. Root supplies private candidate/interpreter paths; public docs use placeholders.

## Phase A — source combination (10 cases)

A01 inventory includes tracked source; A02 includes relevant untracked source/tests; A03 excludes profiles/keys; A04 excludes models/recovery/cache/env; A05 snapshot hashes equal input; A06 all five runtime hunks applied; A07 non-font chat preflight hunks preserved; A08 four report-test changes preserve local additions; A09 no parent writes during snapshot; A10 fresh drift detection rejects changed inputs before integration. Pass requires exact deterministic inventory/diff evidence, not a guessed file count.

## Phase B — combined addon tests (14 cases)

B01 full unit collection includes local Qt/preflight tests; B02 full combined suite no unexplained failures/skips; B03 native severity INFO/WARNING/ERROR; B04 traceback preserved; B05 repeated setup once; B06 worker-thread native Console; B07 launcher exec with/without file; B08 Initialize/direct toggle before logging; B09 stdio import inert/root preserved; B10 actual chat fonts inherited10pt; B11 local128x64PNG with ink/default32ptbold; B12 toolbar synchronization avoids start/stop reentry; B13 existing guarded HTTP-Python default denial and local document contract retained; B14 critical lint plus independent exact-diff review. Use existing tests; do not invent a large duplicate suite. Parent preflight async/Future behavior must be included, not rewritten.

Exact whole-suite shape after verified interpreter discovery:
`PYTHONDONTWRITEBYTECODE=1 QT_QPA_PLATFORM=offscreen <bundled-python> -c 'import sys; sys.path.append("<test-site-packages>"); import pytest; raise SystemExit(pytest.main(["-q", "-p", "no:cacheprovider", "--tb=short", "tests/unit"]))'` from private candidate. Record actual executable/environment/count/duration privately and sanitized result publicly. Native harness requires separate owned profile/config/temp and no user server/models/provider calls.

## Phase C — native candidate (12 cases)

C01 provenance includes exact upstream fix or reviewed backport; C02 architecture/dependencies match; C03 artifact hash/signature verified; C04 installed bundle unchanged; C05 valid copy normal native open; C06 valid recovery same CWD; C07 valid recovery different CWD; C08 decoy same basename cannot redirect check; C09 spaced/Unicode/relative path semantics; C10 invalid ZIP remains rejected; C11 invalid XML remains rejected; C12 copied persisted Corrupted state is explicitly handled/tested. Candidate source-only or unavailable runtime is HOLD for runtime cases. Do not alter real recovery data or original model.

## Phase D — integration/activation preparation (10 cases)

D01 root fresh parent hashes; D02 exact patch apply-check; D03 only ten scoped files changed (five runtime, four report regression/fixture files, one local deterministic preflight-test correction); D04 unrelated parent inventory preserved; D05 candidate/resulting-runtime hashes match; D06 installed signed bundle unchanged; D07 no live process restart/stop/reload; D08 prepared backup preserves Name/FileName/Modified by design (actual user save NOT RUN without authority); D09 active-module/runtime acceptance explicitly HOLD; D10 task-only publication/privacy/links/staged review. Disk integration success is not loaded-code success.

## Performance and data boundaries

Measure combined test/native startup durations and log/font smoke performance where existing harness supports them. No new performance redesign. Full geometry Shape.isValid is a separate unresolved model gate; it is not required to validate logging/fonts. Database migration tests NOT APPLICABLE (no database/schema change). Native GUI is the appropriate Qt alternative to browser E2E; no macOS accessibility scan.

## Test strategy simulation

C1 feasibility99, C2 correctness98, C3 completeness97, C4 risk99, C5 maintainability98 (>95 each). Basis: exact current-source inventory; existing phase intent tests; real isolated GUI; candidate positive/negative recovery controls; clear source/disk/loaded/runtime distinction. These are test-plan judgments; no tests are claimed completed. Results and infeasible exact next steps will replace NOT RUN only when evidenced.

[ID](../dossiers/ID_installed_report_fix_2026-09-06.md) · [PD](../plan/PD_installed_report_fix_2026-09-06.md)

## External plan/ID/TD review

`agy --help` succeeded; `agy --mode plan --print-timeout 1m0s --print=<self-contained text>` returned0. It reviewed supplied text only, without files/tools/tests; no session ID exposed. Accepted: non-target manifest equality, rollback/drift checks, candidate ARM64/signature provenance, explicit Unicode-path fixture, and persisted-Corrupted-status boundary. Existing pre-code gate already covers snapshot hashing and all four actual team roles.

Corrected/rejected review inaccuracies: public base051d161 is not the combined dirty-parent base; the native saveCopy operation was not shown to change identity—the existing save_document handler explicitly assigns FileName afterwards; two documents are modified, not simultaneously active; existing native GUI harness is available, so absence of automation is not established. Full geometry is a separate unresolved model gate, not a new requirement to rewrite logging. Invented file URLs, new team roles and extra generic approval requirements are not adopted. No live backup, restart, core/cache change or publication of dirty features was authorized by review.

## First execution checkpoint

Root captured344 private baseline files and324 code/test candidate files (4.1MB), excluding CAD, .DS_Store, editor/agent cache, bytecode and generated test artifacts. Candidate overlay adds only the existing5runtime/4test changes. An initial nested git-apply returned0 while skipping paths; a helper-exists postcondition caught this. Root used explicit patch-p1 dry-run/apply and verified actual changes before releasing tests. Exit0 alone was not accepted as successful integration. No parent file writes occurred at this checkpoint.

New live read-only evidence reports five documents, two modified; no live model action authorized. Initial recovery inventory confirmed two Corrupted records; a later read-only check found both had become Created while the existing application ran. Latest inventory is five Created/two Failure. Root did not write the live cache; the cause remains unverified. A hash-guarded copy precondition refused the drift. No cleanup or status reset is authorized; fresh inventory is required.

## Harness safety additions before implementation

The portable report-view and recovery harnesses receive intent tests for exclusive evidence root creation, refusal of existing/non-isolated roots, sanitized child environment, containment before native effects, fixed private output location, version/provenance binding, exit/result validation, bounded timeout/owned-child cleanup, no GUI import during static/unit checks, and preservation of unrelated runtime state. Worker records exact supported cases; unsupported full recovery/state transitions remain explicit HOLD. Recovery acceptance uses a genuine isolated GUI, not unverified C++ ABI calls.

## First combined execution result

1991 passed, 3 subtests passed and 1 failed in 200.96 s; zero skipped. The sole failure is the existing preflight-controller real-Qt late-result test: its 30 ms total timeout includes actual Python QProcess startup, while a deliberate 60 ms finish delay intends to force the result-processing phase. Under load, startup can consume the deadline first and report the baseline phase. Worker subsequently verified isolated baseline reproduction; no unrelated production change or silent skip is authorized. This is not a passing combined integration gate.

Root refined durable installation scope to the same nine files tested together: five runtime files and four existing report-fix regression/fixture files. Leaving stale launcher Console doubles behind would invalidate the parent test contract. All nine require fresh baseline hashes and per-file rollback; other parent tests/features remain untouched.

## Baseline failure classification and approved intent correction

The preflight timing test had identical input bytes in parent and candidate. Three isolated baseline runs returned FAIL/PASS/FAIL; candidate returned PASS/PASS/FAIL. The 30 ms deadline can expire during legitimate native QProcess startup before the intended 60 ms artificial result-processing delay. Root authorized a deterministic test-only late-result transition: remaining budget exists until ordinary child finish, then expires explicitly; assert normal child exit/finish before result-timeout checks. The separate real elapsed-time timeout case remains unchanged. This changes no production preflight behavior and does not suppress or deselect tests. Focused/full retest subsequently passed; see current verified outcome.

Durable private parent scope is now ten files: the five report runtime files, four matching report regression/fixture files, and `tests/unit/test_preflight_controller.py` for this local deterministic correction. The tenth file belongs to unpublished preflight work and MUST NOT be copied into this public PR. Its private before/after hashes and independent review belong in the integration evidence.

## Focused correction and harness checkpoint

The corrected real-Qt controller slice passed eight tests in each of five runs (4.68 s, 4.37 s, 4.79 s, 4.70 s, 4.59 s), no skips. Exact local test hash is retained privately. Root independently ran five report-harness guard tests with bundled Python3.11: five passed in 0.34 s. An initial incorrect systemPython3.9 run produced five passes plus five teardown errors because the existing configuration uses PEP604 type syntax; the supported-interpreter rerun is the valid result. This environment mistake was not treated as a production defect. Full combined rerun subsequently passed; see current verified outcome.

The official candidate release is [weekly-2026.09.02](https://github.com/FreeCAD/FreeCAD/releases/tag/weekly-2026.09.02), target commit `c68c452275ec188f35c02f6b30aba880b3783cf1`, prerelease, macOS15 ARM64 asset649432960 bytes. Public release SHA256 is `4cfee0062cd2264878a83e2ad1b1d35839b921179faedb08c4547d8d7ac41996`; private acquisition checksum matched. Signature and the eight recorded genuine-GUI runtime cases subsequently passed; additional path variants remain NOT RUN.

## Native addon GUI acceptance

Exact combined candidate passed in an owned isolated native GUI, normal exit0. Thirteen actual imported module paths and hashes matched the candidate. Corrected INFO and worker-thread INFO matched native PrintMessage color `#102030`; old stderr INFO remained red, true errors/traceback red and warnings yellow, repeated setup emitted once. Application/chat fonts were `.AppleSystemUIFont` at10 pt, local PNG128×64 contained ink (1530 bytes), zero addon Qt messages. No user document, MCP controller or provider was started. Private evidence: `native-gui-v2/{result,exit,launch,startup-marker}.json` beneath the evidence root.

The first owned GUI attempt timed out because FreeCAD executes CLI macros with `__name__="startup"`, not `"__main__"`. A startup marker proved the cause; explicit probe-spec environment autostart and six guard tests corrected the diagnostic. Root closed only that owned process after verifying its argv. This harness failure is distinct from addon behavior. The successful second run closed normally. Full SDK/21-wall benchmark NOT RERUN; it is not needed for the logging/font boundary.

Recovery guard tests: twelve passed under supported Python3.11. Initial systemPython3.9 attempt had twelve passes plus twelve teardown errors from existing>=3.11 code and is not a valid suite result. Compressed-image deep signature verification hit90 s and is INCOMPLETE, not a failed signature. Uncompressed candidate verification subsequently passed: checksum match, read-only private copy, ARM64 and codesign --verify --deep --strict exit0; owned mount detached. The initial read-only cache inventory was two Corrupted, three Created and two Failure. Latest check is five Created/two Failure: two metadata hashes changed during the existing session. No agent live-cache write occurred, but hash preservation cannot be claimed. Root guarded copy refused the mismatch; cause is unverified. Seven semantic cases exist in the native recovery harness; same-CWD, decoy-basename, Unicode and relative-path variants remain NOT RUN unless separately evidenced.

## Independent public harness checks

Root reran all public probe guard tests under bundled Python3.11:18 passed in0.29 s; private JUnit `public-probe-guards.xml`. Critical Ruff passed for all four new Python files using the installed CLI. The bundled interpreter's `-m ruff` was unavailable; switching to the installed CLI resolved the tooling gap without package installation or a skipped check. Root independently matched the native GUI PASS/exit0 and all13 current module hashes. The prepared ten-file integration command still passes the344-file baseline check; root subsequently executed and verified disk integration, as recorded below.

## Final addon integration evidence

Full combined unit suite:1992 passed plus3 subtests in182.39 s, JUnit1995 and zero failures/errors/skips. Explicit native preflight selection10 passed24.91 s; broad SDK and21-wall benchmark remain NOT RERUN. Root checked344 baseline entries: seven expected hash changes and337 unchanged entries, plus three new files for ten scoped integration files. Private `installation-gate.json`, `installation-result.json`, preimage and `exact-parent-integration.diff` bind disk result and rollback. Actual installed-target regression63 passed9.77 s, zero skips, private `installed-targeted.xml`. Authenticated MCP check still reports55 tools and five documents/two modified. Status PASS_DISK_ONLY_RUNTIME_NOT_RELOADED; the corrected local preflight test is excluded from publication.

## Genuine recovery GUI discovery

The installed1.1.3 real recovery GUI reproduced BOTH “Original project file is corrupted” and “Error reading from file” with a valid synthetic tiny-box project outside CWD. Created metadata became Corrupted, dialog text was red (`#aa0000`), input hash remained unchanged and owned process exited0. This supersedes the older generic-read-error NOT REPRODUCED status for this recovery route; it does not establish all causes of generic read errors. Final hash-bound candidate A/B passed with the same copied user project; both legacy error strings disappeared, Created stayed Created and report text was black. Each control required an actual ReportOutput widget and normal exit0.

## Public CI contract

Root added the two diagnostic guard-test files to the existing security-regression slice and the two harnesses to its fatal-Ruff check (three scoped workflow lines). Public CI outcome remains pending publication; local18 guard tests/Ruff are evidenced separately. If synchronizing the workflow into the dirty installed checkout, apply only the three reviewed lines after fresh comparison; do not replace unrelated local workflow changes.

## Live cache drift boundary

Fresh root read-only verification found the two initially Corrupted metadata records now Created; latest total is five Created/two Failure. Their hashes changed while the user's existing process remained running. Root's guarded copy precondition rejected the mismatch before copying. No root/worker live-cache mutation occurred; the cause is unverified and must not be labeled autosave without evidence. Cache hash preservation is NOT PASS, and any migration/status repair remains HOLD pending fresh inventory and explicit scope. Evidence `live-cache-drift.json` stays private.

## Final genuine recovery GUI matrix

Final script SHA256 `a093aac9188cefdbdbd451a920ce0d3ecfee698ddc9829681212e9ceb6ec3c3e` binds eight cases, all statusPASS/exit0 and exactly one ReportOutput widget. Peer review found that absent ReportOutput could falsely appear error-free; the harness now requires a widget and old/new controls were rerun on the corrected bytes.

| Case | Observed native result | Acceptance meaning |
|---|---|---|
| Installed1.1.3, copied actual project outside CWD | both exact screenshot errors; Created→Corrupted; red `#aa0000` | PASS reproduction of legacy defect, not legacy repair |
| Candidate, same copied bytes outside CWD | neither error; Created retained; black `#000000` | PASS corrected recovery validation |
| Candidate with copied persisted Corrupted | Corrupted retained/red | PASS known stored-status boundary, not auto-repair |
| Candidate with revalidated fixture metadata | Created/no errors | PASS isolated status fixture only; no live-cache edit |
| Missing file | Corrupted/warning | PASS negative control |
| Invalid ZIP | Corrupted/warning | PASS negative control |
| Invalid XML | Corrupted/warning | PASS negative control |
| Accept copied actual project |163 objects,64 links, GUI document present, no Invalid object state or report errors | PASS real recovery-open inventory; not exhaustive topology validation |

Actual candidate `App.Version` is26.3.0, build20260902, source`c68c452275ec188f35c02f6b30aba880b3783cf1`; package Info.plist26.3.0d36 is a separate version field. Original/input copy hashes remained unchanged. Candidate deep strict signature passed again after native execution; installed app was not replaced. Private `native/native-gui-case-matrix.json` and postruntime preservation evidence retain exact paths, launch/result hashes and exits.

**NOT RUN:** same-CWD candidate variant, decoy basename, Unicode/spaced source path, relative source path, exhaustive Shape.isValid. The bounded eight-case semantic acceptance does not close every preplanned C-case or authorize production candidate installation. No silent skips: these are explicit unexecuted scenarios.

Live cache chronology: five of seven baseline metadata records unchanged; two became Created with modification times08:23:18/19, before the first diagnostic GUI08:24 and recovery probe08:31. Writer was not directly observed; do not label the cause. Hash-guarded copy refused before copying. Latest inventory remains five Created/two Failure; migration is HOLD and must use fresh evidence.

Final root verification: all eight native cases and exact final harness hash independently matched. Public guard suite18 passed0.58 s, zero skips; fatal Ruff passed. Installed stable application deep strict signature also passed after diagnostics. This closes the signed-app preservation check without authorizing replacement or activation.
