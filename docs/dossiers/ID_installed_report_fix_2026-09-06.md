# Implementation dossier: installed report fix

## Current verified outcome

**PASS_DISK_ONLY_RUNTIME_NOT_RELOADED.** The corrected combined suite passed1992 tests plus3 subtests in182.39 s (JUnit1995, zero failures/errors/skips). Selected native preflight checks passed10 in24.91 s. Exact combined native GUI passed with13 imported-module hashes and normal exit0. Root integrated ten scoped files: seven expected baseline changes plus three new files;337 other baseline files are unchanged. Actual installed-target tests passed63 in9.77 s, zero skips. Authenticated MCP check and read-only document inventory still pass. Live activation remains HOLD: no reload, stop or restart was performed. Eight genuine recovery-GUI cases passed with normal exit0 and a confirmed ReportOutput widget: legacy defect reproduced, candidate valid/status/negative/accept controls passed. Four additional path variants and exhaustive geometry remain NOT RUN. The native candidate is not installed.

Prepared before code/integration,2026-09-06; predecessor [ID](ID_report_errors_2026-09-05.md). Fresh publication base051d161 already contains the logging/font fix. The new implementation work is exact-parent combination, verification and root-only ten-file integration preparation, not redeveloping those fixes.

## Phase A — exact snapshot and surgical overlay

Root reads current parent status plus tracked/untracked source/test inventory; freezes a private manifest using relative paths, byte lengths and SHA256. Include the current preflight/controller/Future dispatch and its test files; exclude CAD/doc recovery data, user profiles, tokens, config values, environments and generated caches. The private snapshot must be separate from the clean public worktree. Compare every copied file hash against input. Any concurrent parent drift is a new boundary, not automatically accepted.

Overlay existing tested changes only: console_logging.py new helper; mcp_server_http.py replace root basicConfig with namespace helper; InitGui.py install helper in Initialize/direct toggle; chat_widget.py change only two font constructions/remove now-unused QFont alias; llm/client.py default-font32ptbold. Parent chat_widget now has asynchronous code-review changes; retain those bytes/hunks. Add the four corresponding report-fix test files/fixture hunks without overwriting local test additions. Review before/after diff; no parent write by workers.

## Phase B — combined verification

Logging worker runs the complete unit collection and suite in the private candidate using verified bundled interpreter with isolated state. Ensure local dispatcher/preflight/toolbar/document/guarded HTTP-Python/start-helper tests are present. Compare total to collection rather than expecting1642. New failures are classified by source and reproduction; do not modify unrelated features opportunistically. Run critical lint and focused logging/font/startup regression. Run owned isolated native FreeCAD GUI without server/user models: native control colors, corrected INFO/WARNING/ERROR/traceback/thread routing, idempotent installation, actual ChatDockWidget default fonts/10pt and local128x64PNG. No paid provider call or accessibility query. Independent reviewer signs the actual candidate hash/diff and boundaries.

## Phase C — native recovery candidate

Recovery worker identifies a primary-source candidate containing absolute-path fix1d70f987abbb429e5bcd1411fe067ac43a54785f or documents a reproducible backport need. Record exact version, architecture, source/build reference, download/artifact hashes and signed-app isolation. Only a separate owned candidate may be exercised. Before using any new executable, root reviews provenance and launch scope. Tests use copied valid/invalid archives and copied synthetic recovery metadata: same/different CWD, decoy basename, relative Unicode/spaced path, bad ZIP/XML, persisted Corrupted status and normal/recovery open. Source inclusion alone is not candidate runtime PASS. No installed signed package or actual recovery cache mutation.

## Phase D — root integration and activation preparation

Only root may integrate the five runtime changes, four corresponding test/fixture files and the separately authorized local preflight test correction after combined PASS and fresh parent hash/apply checks. Preserve unrelated parent changes and record resulting per-file hashes; if parent drifted, rebuild/retest the affected combined candidate first. Runtime modules loaded before file changes stay old; no reload/server stop/restart is part of this gate. Prepare a concrete activation handoff with unsaved-document protection and native saveCopy identity checks. Existing save_document changes FileName and must not be used as a transparent backup. Any native copy/save of current user state waits for separate authority. Later activation requires explicit targeted FreeCAD restart confirmation and authenticated discovery/read-only tool plus actual logger/font/toolbar acceptance. Native-core installation is a distinct decision. Root publishes only new task artifacts, never the copied dirty parent.

## Simulation before implementation

These are bounded engineering design judgments, not empirical probabilities or test results. Canonical criteria C1 technical feasibility, C2 logical correctness, C3 completeness, C4 risk control, C5 maintainability all exceed95 for the authorized preparation plan. Activation and native deployment cannot be scored as completed because authority/candidate proof is absent.

| Phase | C1 | C2 | C3 | C4 | C5 | Grounding |
|---|---:|---:|---:|---:|---:|---|
| A | 99 | 98 | 97 | 99 | 98 | existing patch, exact manifest, unrelated-hunk preservation |
| B | 98 | 98 | 97 | 99 | 98 | existing tests/native harness, collection comparison, private fixture |
| C preparation | 97 | 97 | 96 | 99 | 97 | known upstream fix, isolated candidate, honest blocked-runtime path |
| D disk integration preparation | 98 | 98 | 97 | 99 | 98 | root ownership, fresh drift gate, no implicit activation |
| Whole authorized plan | 98 | 98 | 97 | 99 | 98 | dependency ordering and explicit unresolved boundaries |

Additional TCCode criteria: integration fit98, safety99, testability98, performance97, observability99, rollback99: narrow existing changes, no live mutation, explicit suite/native evidence and source manifests. Re-score if root/snapshot/worker discovers a new implementation boundary. Candidate deployment and live activation remain HOLD, not passed by these scores.

[ARD](ARD_installed_report_fix_2026-09-06.md) · [TRD](TRD_installed_report_fix_2026-09-06.md) · [PD](../plan/PD_installed_report_fix_2026-09-06.md) · [TD](../tests/TD_installed_report_fix_2026-09-06.md)

## External plan/ID/TD review

`agy --help` succeeded; `agy --mode plan --print-timeout 1m0s --print=<self-contained text>` returned0. It reviewed supplied text only, without files/tools/tests; no session ID exposed. Accepted: non-target manifest equality, rollback/drift checks, candidate ARM64/signature provenance, explicit Unicode-path fixture, and persisted-Corrupted-status boundary. Existing pre-code gate already covers snapshot hashing and all four actual team roles.

Corrected/rejected review inaccuracies: public base051d161 is not the combined dirty-parent base; the native saveCopy operation was not shown to change identity—the existing save_document handler explicitly assigns FileName afterwards; two documents are modified, not simultaneously active; existing native GUI harness is available, so absence of automation is not established. Full geometry is a separate unresolved model gate, not a new requirement to rewrite logging. Invented file URLs, new team roles and extra generic approval requirements are not adopted. No live backup, restart, core/cache change or publication of dirty features was authorized by review.

## First execution checkpoint

Root captured344 private baseline files and324 code/test candidate files (4.1MB), excluding CAD, .DS_Store, editor/agent cache, bytecode and generated test artifacts. Candidate overlay adds only the existing5runtime/4test changes. An initial nested git-apply returned0 while skipping paths; a helper-exists postcondition caught this. Root used explicit patch-p1 dry-run/apply and verified actual changes before releasing tests. Exit0 alone was not accepted as successful integration. No parent file writes occurred at this checkpoint.

New live read-only evidence reports five documents, two modified; no live model action authorized. Initial recovery inventory confirmed two Corrupted records; a later read-only check found both had become Created while the existing application ran. Latest inventory is five Created/two Failure. Root did not write the live cache; the cause remains unverified. A hash-guarded copy precondition refused the drift. No cleanup or status reset is authorized; fresh inventory is required.

## Guarded native harness implementation gate

Two portable diagnostic scripts and intent tests are authorized in the clean publication worktree: `scripts/check_report_view_native.py` for the actual Qt report/font boundary, and `scripts/check_recovery_native.py` for the genuine document-recovery GUI. The latter uses an exclusive profile, synthetic/copy-only fixtures, effective cache/data/parameter containment before delayed recovery, actual dialog inspection, a bounded owned child and negative ZIP/XML controls. No FFI or actual recovery-cache edits. Code/static tests are released; candidate execution requires root provenance/signature and launch clearance. These additions make the isolated checks reproducible; they do not replace or copy parent preflight features.

## Integration contract refinement

Root approved ten durable parent files: five runtime files plus their four already-merged regression/fixture files. This keeps the installed test contract aligned with the tested snapshot. The first combined run has one existing Qt timing failure; installation was held until the baseline defect was reproduced and the deterministic correction passed. No skip, relaxed unrelated assertion or unreviewed local-feature patch is an acceptable substitute for evidence.

## Deterministic local regression correction gate

Identical baseline/candidate timing-test bytes failed intermittently in both. Root explicitly approved the bounded late-result test correction described in TD; no production preflight change. Parent integration therefore covers ten files, with the tenth local test kept outside publication. This supersedes the earlier nine-file contract; fresh manifest/rollback covers all ten. Focused/full GREEN and root exact-candidate review subsequently passed; root completed disk integration.
