# Technical requirements: installed report fix

## Current verified outcome

**PASS_DISK_ONLY_RUNTIME_NOT_RELOADED.** The corrected combined suite passed1992 tests plus3 subtests in182.39 s (JUnit1995, zero failures/errors/skips). Selected native preflight checks passed10 in24.91 s. Exact combined native GUI passed with13 imported-module hashes and normal exit0. Root integrated ten scoped files: seven expected baseline changes plus three new files;337 other baseline files are unchanged. Actual installed-target tests passed63 in9.77 s, zero skips. Authenticated MCP check and read-only document inventory still pass. Live activation remains HOLD: no reload, stop or restart was performed. Eight genuine recovery-GUI cases passed with normal exit0 and a confirmed ReportOutput widget: legacy defect reproduced, candidate valid/status/negative/accept controls passed. Four additional path variants and exhaustive geometry remain NOT RUN. The native candidate is not installed.

Prepared before implementation,2026-09-06. [Previous TRD](TRD_report_errors_2026-09-05.md) remains historical; this successor controls the combined current-parent task.

| ID | Required behavior | Evidence |
|---|---|---|
| R1 | Current dirty source captured exactly | code-only relative-path/hash inventory before/after snapshot |
| R2 | Existing five-file correction applied surgically | reviewed overlay diff; unrelated preflight/chat hunks preserved |
| R3 | Current local feature behavior retained | full combined unit inventory incl dispatcher, toolbar, document, HTTP-Python and preflight tests |
| R4 | Native GUI log/font contract still works | isolated Console colors, worker INFO, traceback, repeat setup, actual fonts/local PNG |
| R5 | No real provider calls/live parameter access | empty private config/profile; explicit native process ownership |
| R6 | Integration cannot overwrite later parent edits | fresh source hashes and patch check immediately before root mutation; fail on drift |
| R7 | Recovery candidate provenance and isolation | exact build/source inclusion/architecture/hash; separate destination; installed app unchanged |
| R8 | Candidate fixes path semantics without hiding corruption | same/different CWD, decoy basename, invalid ZIP/XML, standard open and recovery fixture |
| R9 | Recovery metadata and unsaved state preserved | no live writes; copied status-transition tests; native saveCopy plan preserves identity |
| R10 | Honest activation/publication boundary | disk integration separate from loaded code; restart HOLD; task-only public artifacts |

## Current evidence and gaps

Parent changed materially since previous analysis: current diff includes core preflight/access/executor, Future dispatcher handling, chat asynchronous code-review paths, review dialog, installer and their tests. These are existing user work, not this task's implementation scope. Only chat font hunks intersect one five-file patch target. Root must inspect actual overlay success and fresh hashes rather than assuming old apply-check remains valid.

Native1.1.3 checker previously reproduced basename-dependent false corruption; official weekly2026.09.02 macOS15 ARM64 candidate selected and privately acquired; artifact checksum and read-only mount passed, deep strict signature passed on the uncompressed copy; eight semantic GUI cases subsequently passed; untested path variants remain explicit. The generic read error was subsequently reproduced with the false corruption warning in the real isolated legacy recovery GUI; candidate A/B subsequently passed for the copied project. Complete Shape.isValid remains NOT_COMPLETED_CPU_BOUND. No archive pass or candidate source commit alone closes native runtime gates.

No API/schema/database/migration change requested. Tests use existing FreeCAD bundled Python/Qt when feasible, with the entire local suite present. Critical lint, exact source diff, independent review, original/installed-app preservation and explicit skip reporting remain required.

[ARD](ARD_installed_report_fix_2026-09-06.md) · [ID](ID_installed_report_fix_2026-09-06.md) · [TD](../tests/TD_installed_report_fix_2026-09-06.md)

## External plan/ID/TD review

`agy --help` succeeded; `agy --mode plan --print-timeout 1m0s --print=<self-contained text>` returned0. It reviewed supplied text only, without files/tools/tests; no session ID exposed. Accepted: non-target manifest equality, rollback/drift checks, candidate ARM64/signature provenance, explicit Unicode-path fixture, and persisted-Corrupted-status boundary. Existing pre-code gate already covers snapshot hashing and all four actual team roles.

Corrected/rejected review inaccuracies: public base051d161 is not the combined dirty-parent base; the native saveCopy operation was not shown to change identity—the existing save_document handler explicitly assigns FileName afterwards; two documents are modified, not simultaneously active; existing native GUI harness is available, so absence of automation is not established. Full geometry is a separate unresolved model gate, not a new requirement to rewrite logging. Invented file URLs, new team roles and extra generic approval requirements are not adopted. No live backup, restart, core/cache change or publication of dirty features was authorized by review.

## First execution checkpoint

Root captured344 private baseline files and324 code/test candidate files (4.1MB), excluding CAD, .DS_Store, editor/agent cache, bytecode and generated test artifacts. Candidate overlay adds only the existing5runtime/4test changes. An initial nested git-apply returned0 while skipping paths; a helper-exists postcondition caught this. Root used explicit patch-p1 dry-run/apply and verified actual changes before releasing tests. Exit0 alone was not accepted as successful integration. No parent file writes occurred at this checkpoint.

New live read-only evidence reports five documents, two modified; no live model action authorized. Initial recovery inventory confirmed two Corrupted records; a later read-only check found both had become Created while the existing application ran. Latest inventory is five Created/two Failure. Root did not write the live cache; the cause remains unverified. A hash-guarded copy precondition refused the drift. No cleanup or status reset is authorized; fresh inventory is required.
