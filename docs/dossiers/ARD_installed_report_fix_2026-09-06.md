# Architecture requirements: installed report fix

## Current verified outcome

**PASS_DISK_ONLY_RUNTIME_NOT_RELOADED.** The corrected combined suite passed1992 tests plus3 subtests in182.39 s (JUnit1995, zero failures/errors/skips). Selected native preflight checks passed10 in24.91 s. Exact combined native GUI passed with13 imported-module hashes and normal exit0. Root integrated ten scoped files: seven expected baseline changes plus three new files;337 other baseline files are unchanged. Actual installed-target tests passed63 in9.77 s, zero skips. Authenticated MCP check and read-only document inventory still pass. Live activation remains HOLD: no reload, stop or restart was performed. Eight genuine recovery-GUI cases passed with normal exit0 and a confirmed ReportOutput widget: legacy defect reproduced, candidate valid/status/negative/accept controls passed. Four additional path variants and exhaustive geometry remain NOT RUN. The native candidate is not installed.

Prepared before implementation, 2026-09-06. Successor to [the report-error ARD](ARD_report_errors_2026-09-05.md) and the prior local installed-code analysis (private mapping retained locally). Fresh publication worktree base051d161; parent remains dirty6e51894 with additional preflight changes since that analysis.

## Outcome and boundaries

Prepare and test the exact current installed-source combination with the existing five-file report-view correction; root alone may integrate those five runtime files, four matching regression/fixture files and the separately authorized local preflight timing-test correction after gates. Prepare a separate native recovery candidate and reproducible tests. Activation of the running user instance, restart/server stop, hot reload, signed-app replacement, model or recovery-cache writes are not authorized. No new backup API, logging redesign or speculative model repair is required.

The report fix already exists: namespace-local Console handler, launcher/Initialize/direct-toggle configuration and inherited fonts. Parent now also contains asynchronous preflight/controller changes in chat_widget/executor; the old claim that all four existing patch targets equal parent HEAD is stale. Capture current bytes before overlay and preserve every unrelated hunk. A clean published-checkout replacement would remove needed local behavior.

## Architecture and ownership

Parent code → private code-only inventory/snapshot → surgical overlay of five runtime/four matching test changes → combined tests and isolated native GUI → root integration after fresh hash checks. Native recovery candidate is a separate side-by-side artifact; it never replaces the signed installed app. Production/freecad-ai source already contains the report fix, so publication contains only this task's new reusable artifacts/documentation, never copied parent features.

Fixed four-person team: root owns snapshot, scope, integration and publication; orchestrator owns dossiers/gates/docs; logging worker owns combined tests/native GUI; recovery reviewer owns native candidate preparation and independent review. No new agents. Thorough stages retained with roles reused; initial TD precedes implementation. Existing repository rules and explicit authority outrank generic workflow templates. No docs/settings directory found.

## Acceptance boundaries

- Combined snapshot includes all current source/tests, including preflight and prior Qt/toolbar/document/HTTP-Python work; no profiles, keys, CAD files, recovery cache or environment directories.
- Five runtime changes preserve all parent-specific logic and map native log levels correctly; widget font size and local PNG behavior unchanged.
- Complete test inventory and results belong to the exact combined hash manifest; earlier1642PASS is historical scoped-base evidence only.
- Native recovery candidate validity is version/architecture/hash-bound; normal/recovery valid fixtures independent of CWD, genuinely invalid fixtures rejected. Existing Corrupted status requires its own copied-metadata exercise.
- Backup preparation must preserve Name/FileName/Modified. Existing save_document assigns FileName after saveCopy, so it is not an identity-preserving backup operation.
- Task operations must preserve original/live documents, parent unrelated bytes and installed signed app. No agent live-cache mutation occurred, but two live metadata hashes drifted independently; cache-hash preservation is not PASS. Unexercised activation/core installation/full geometry gates stay HOLD.

[TRD](TRD_installed_report_fix_2026-09-06.md) · [ID](ID_installed_report_fix_2026-09-06.md) · [PD](../plan/PD_installed_report_fix_2026-09-06.md) · [TD](../tests/TD_installed_report_fix_2026-09-06.md)

## External plan/ID/TD review

`agy --help` succeeded; `agy --mode plan --print-timeout 1m0s --print=<self-contained text>` returned0. It reviewed supplied text only, without files/tools/tests; no session ID exposed. Accepted: non-target manifest equality, rollback/drift checks, candidate ARM64/signature provenance, explicit Unicode-path fixture, and persisted-Corrupted-status boundary. Existing pre-code gate already covers snapshot hashing and all four actual team roles.

Corrected/rejected review inaccuracies: public base051d161 is not the combined dirty-parent base; the native saveCopy operation was not shown to change identity—the existing save_document handler explicitly assigns FileName afterwards; two documents are modified, not simultaneously active; existing native GUI harness is available, so absence of automation is not established. Full geometry is a separate unresolved model gate, not a new requirement to rewrite logging. Invented file URLs, new team roles and extra generic approval requirements are not adopted. No live backup, restart, core/cache change or publication of dirty features was authorized by review.

## First execution checkpoint

Root captured344 private baseline files and324 code/test candidate files (4.1MB), excluding CAD, .DS_Store, editor/agent cache, bytecode and generated test artifacts. Candidate overlay adds only the existing5runtime/4test changes. An initial nested git-apply returned0 while skipping paths; a helper-exists postcondition caught this. Root used explicit patch-p1 dry-run/apply and verified actual changes before releasing tests. Exit0 alone was not accepted as successful integration. No parent file writes occurred at this checkpoint.

New live read-only evidence reports five documents, two modified; no live model action authorized. Initial recovery inventory confirmed two Corrupted records; a later read-only check found both had become Created while the existing application ran. Latest inventory is five Created/two Failure. Root did not write the live cache; the cause remains unverified. A hash-guarded copy precondition refused the drift. No cleanup or status reset is authorized; fresh inventory is required.
